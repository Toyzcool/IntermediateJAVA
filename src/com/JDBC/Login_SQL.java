package com.JDBC;

public class Login_SQL {
	/*
	 * 用于记录写入SQL的语句
	 */
	/*
	 * CREATE TABLE user(
	 * 	id int(11) AUTO_INCREMENT,
	 * 	name varchar(30),
	 * 	password varchar(30),
	 * 	PRIMARY KEY(id)
	 * );
	 * INSERT INTO user VALUES(null,'toyz','admin123');
	 */
}
